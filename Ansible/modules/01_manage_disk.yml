---
- name: Configure NVMe disk a single PV 
  hosts: all
  become: yes
  tasks:

    - name: Create Physical Volume
      lvg:
        pvs: /dev/nvme0n1
        vg: vg_nvme
        state: present
        
